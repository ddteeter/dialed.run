# Deferred register

Everything the PR #2–#5 review surfaced that was **not** fixed in the PR that
found it, and why. This exists because "flagging it for later" in a review
thread is not a plan — the thread scrolls away and the finding goes with it.

Not a backlog of ideas. Every row is a known defect, a known omission, or a
decision someone deliberately postponed, with the thing that should trigger
picking it up. Post-MVP *epics* live in `post-mvp.md`; this is pre-launch debt.

**Update this in the same commit that defers something.** A review reply
saying "flagging it" and no row here is the failure mode this file exists to
prevent.

**And retire a row in the commit that fixes it.** Audited 2026-09-07 after
the lane merges: eight rows described work that was already done, some of it
months of reading apart from the fix. A register that claims open work is
untrustworthy in both directions — you stop believing the rows that are
real. Retired rows are struck through and kept, because "why isn't this a
problem any more" is a question worth being able to answer.

Status: `blocked` (needs a human decision) · `ready` (decided, just not done)
· `watch` (correct for now, revisit on a named trigger).

---

## Correctness — fix before launch

| # | What | Where | Status | Trigger |
|---|---|---|---|---|
| D-1 | A transient network failure marks a working Strava connection `broken` and emails the user to reconnect. The `catch` is unconditional; only 400/401 on refresh is terminal. Needs a typed error at the `StravaApi` boundary *and* a product call on when repeated transient failure becomes broken. | `modules/runs/strava/oauth.ts:113` | blocked | Before Strava OAuth is enabled for real users. |
| D-2 | ~~Manual run submission is not idempotent.~~ **Done, and generalised.** `createManualRun` was the worked example; task 108 extended it to garment create, entry photos and file imports, and pinned the paths that were already safe by construction. See `test/idempotency.test.ts`. | `modules/runs/service.ts` | done | — |
| D-3 | Photo resize decodes into a 128MB isolate with only a *compressed* size cap. A 10MB JPEG can decode to ~96MB. Three options in the thread; the cheapest (bound the decoded pixel budget) removes the OOM class without changing the upload contract. | `modules/closet/photos.ts:111` | blocked | Pick one before real photos. |
| D-4 | Strava webhook does two D1 round-trips before responding, against a disable-on-slow-response policy. Should enqueue and let the consumer do the lookup and dedupe. Changes the queue message shape, so it is the first real test of resilience law 9. | `modules/runs/strava/webhook.ts:52` | ready | Owner go-ahead; do it with the notification consumer in view. |
| D-5 | Weather is resolved at `startedAt` only. A 9–11am run is remembered as a 9am run, the verdict reflects the whole run, and the model learns the wrong association — this poisons the call epic's training signal rather than just displaying a wrong number. Sample start/mid/end and store the range; do **not** average, which smooths away the extremes people actually judge by. | `modules/runs/service.ts:80` | blocked | Next schema batch. Highest value item here. |

## Unfinished wiring

| # | What | Where | Status | Trigger |
|---|---|---|---|---|
| D-6 | `user_profiles.temp_unit` and `.distance_unit` **already exist** and nothing reads them, so every temperature renders in Celsius. `lane 104`'s `lib/temperature.ts` already has `formatTemp(tempC, unit)` — but all four call sites pass a hardcoded `"f"`. The work is wiring the stored preference through, not writing a formatter, and it is **not** a schema change (I said twice that it was; the columns were already there). | `lib/temperature.ts`, feed + closet temp display | ready | **After the lanes merge** (owner decision) — doing it in 101 and 104 separately would put two copies of the same function on the same shared path. |
| D-7 | The `"c" \| "f"` union is a bare string union in `lib/temperature.ts` and a column enum in the schema — two independent lists. One `z.enum` in `lib/contracts.ts` with the type derived via `z.infer`, so the validator, the type and the stored vocabulary are one thing. | `lib/temperature.ts:38` | ready | With D-6, same commit. |
| D-8 | `itemFlags[].note` is read back and displayed but the UI never sends it — the write half is missing. Either wire the input or drop the field until there is one. | `modules/feed/functions.ts`, `routes/feed/verdict.$entryId.tsx` | blocked | Decide: is a per-item note a v1 feature? |
| D-9 | ~~Retiring a garment gives no feedback.~~ **Answered by design, not by a toast.** Retiring lands on the closet with retired items shown, so the item is visibly *there* and marked `[Retired]` — stronger than announcing something happened, because it also shows where the thing went. The general transient-feedback gap is design-delta #3, narrowed by round 4 to actions with no screen to land on. | `routes/closet/$itemId.tsx` | done | — |

## Duplication still standing

| # | What | Where | Status | Trigger |
|---|---|---|---|---|
| D-10 | ~~UI groups implemented twice.~~ **Done** — `uiGroups`/`uiGroupFor` live in `lib/contracts.ts`; lane 104 deletes its copy on rebase. | — | done | — |
| ~~D-10-old~~ | UI groups are implemented twice — `modules/closet/service.ts` (`computeUiGroup`) and `modules/feed/groups.ts` (`uiGroupFor`). Both derive the same table from `contracts.md`; lane 104's file header says so outright. Same concept, so they merge into `lib/`, not a case of two groupings that coincide. | both | ready | When 101 and 104 are both on main. |
| D-11 | ~~`GarmentForm` wiring is duplicated between the closet routes.~~ **Done.** The duplicated block was the identity-first product resolution (D-27), copied down to a comment reading "see new.tsx for the identical note" — two copies of the rule that decides whether a garment gets a canonical product, where one route linking and the other not is invisible until half the closet has no social proof. Now `garmentWithResolvedProduct`. | `modules/closet/form-mapping.ts` | done | — |
| D-12 | ~~Product brand+name normalisation is copy-pasted.~~ **Was already done when this row was written down.** `normalizeIdentity` is one named function in `lib/normalize.ts`, called from all three product paths, pinned by `test/products/normalize.test.ts`. Fixed during the PR #2-#5 review; the row outlived the fix. | `lib/normalize.ts` | done | — |
| D-13 | ~~`itemLabel()` is re-implemented for alt text.~~ **Done.** `garmentLabel` in `modules/closet/label.ts` is the single rule; `ClosetGrid`'s `itemLabel` is a three-line adapter over it and the detail page calls it directly, so the visible label and the accessible name cannot disagree. | `modules/closet/label.ts` | done | — |
| D-14 | ~~Performance buckets are a TS union and a zod enum.~~ **Done.** `performanceBuckets` is one `z.enum` in `lib/contracts.ts` with the type derived from it; `closet/service.ts` imports the type. Worth remembering as the standing example of what a clone detector *cannot* see — it was two restatements of one set, not copied text (upstream issue #41). | `lib/contracts.ts` | done | — |
| D-24 | ~~**The remaining integer-booleans.**~~ **Done.** `outfit_entries.is_public`, `user_profiles.share_default`/`.onboarding_complete` and `brands.seeded` now use drizzle `mode:"boolean"`, joining `runs.indoor`, `notifications.read` and the `wardrobe_items` flags. One migration, as the trigger asked. Nothing in the codebase compares a boolean column to 0 or 1 any more; anything 105-107 adds should be declared boolean from the start. | `db/schema-core.ts` | done | — |
| D-25 | ~~Adopt `lib/copy.ts` in the auth surfaces.~~ **Done, by deletion.** The inlined `RETRY_GENERIC` was the visible symptom; the real duplication was that sign-in and sign-up were the same page twice, differing by a `name` field and five words. Both now render `AuthPage`, and neither authors an error sentence at all — copy lives in the schema. | `modules/auth/auth-page.tsx` | done | — |
| D-15 | ~~Extension parsing is duplicated.~~ **Done.** `importExtensionOf` lives in `parsers/index.ts` and `imports.ts` imports it; the two error messages stay different on purpose, which was the point. | `modules/runs/parsers/index.ts` | done | — |

## Structural

| # | What | Where | Status | Trigger |
|---|---|---|---|---|
| D-16 | ~~Notifications live inside `modules/runs`.~~ **Done.** `modules/notifications/` exists with its own db/service/functions/components and a top-level route; `modules/runs` reaches it through the barrel (`createNotification`, `notificationInsert`). Landed with lane 102 before 104 added its kinds, which is what the trigger asked for. | `modules/notifications/` | done | — |
| D-17 | ~~Adopt the Forms & failure contract.~~ **Primitive shipped; four forms migrated, one to go.** `ui/form.tsx` + `ui/use-form-submit.ts` carry the whole contract, pinned by `test/ui/form.test.tsx`. Sign-in, sign-up, manual run entry and **the closet's add/edit form** are on it. **What the closet turned out to need was not a migration.** It had no failure path at all: `GarmentForm` was presentational and the route called `garmentFromFormValues` — which ends in `garmentSchema.parse` — inside a `void`-ed async handler, so an invalid garment threw into an unhandled rejection and the button silently did nothing. Reachable by typing an `http://` product link or overrunning any of the four length caps, none of which had a `maxLength`. The error copy did not exist either; six sentences now live in the schema. **Still to go: the verdict photo upload** (`modules/feed/components/VerdictForm.tsx`, 317 lines), which hand-rolls `error` and `photoError` as pink text — the contract's other named violation, since pink is action and never failure — and has no live region at all. | `modules/feed/components/VerdictForm.tsx` | ready | Before lane 105 writes onboarding forms — it will copy whichever pattern it finds. |
| D-18 | ~~Brand seed runs at runtime.~~ **Done.** It is `0002_curated_brand_seed.sql`, a data migration. `seed-brands.ts` now only exports the `CURATED_BRANDS` list, which the migration was generated from and a test still checks against. Nothing seeds on a request path. | `db/migrations/core/0002_*` | done | — |
| D-19 | ~~`env.PHOTOS` holds photos and import files.~~ **Done, and more than asked.** The binding is split: `MEDIA` (indefinite — the app renders it) and `IMPORTS` (30-day lifecycle). `PHOTOS` no longer exists anywhere. Decided before first deploy, which was the trigger. | `wrangler.jsonc` | done | — |

## Design adoption — the artboards exist, the code does not

Created by the round 4 design import (2026-09-07). These are not
design questions — `docs/design-deltas.md` holds those. They are the
build work the answers produced.

| # | What | Where | Status | Trigger |
|---|---|---|---|---|
| D-30 | **The Icon Pack is imported but unused.** `ui/icons.tsx` carries 79 glyphs and exactly one renders anywhere (the notification bell). Screens use text where the pack has a glyph, which is the drift the pack exists to stop, and the longer it runs the more surfaces have to be revisited at once. Nav, action, weather, verdict, social and system glyphs are all usable today. Only the **closet grid** waits on D-33, and only for user-created garments — the tap-list already knows its own types (see D-33). | `src/ui/icons.tsx`, all screens | ready | Any surface a lane touches from now on; audited at the launch gate. |
| D-33 | **Garment type: the model is settled, the surfaces are not.** Design's round 5 answered where it comes from (the product, on match or enrichment — never a user, never a parser), and `products.type` + the match-time cache are in. What is unbuilt is everything that *shows* it: the detail subtitle `type ?? categoryLabel + " · GENERIC"`, and type filter chips built from the types actually present in a category (an all-generic category shows none — correct, not broken). Also unbuilt: enrichment writing `products.type`, which is lane 107 and the only thing that fills the cache for a product nobody has typed. Until then every garment's type is null and the surfaces must render that state without looking broken. | `routes/closet/`, `modules/closet/`, `modules/enrichment/` | ready | With D-30/D-31 for the surfaces; with lane 107 for the source. |
| D-34 | **Split the data tables out of `ui/icons.tsx` and `closet/tap-list.ts`.** Both are a manifest plus ~40 lines of behaviour in one file, and the manifest reads as one giant clone to any semantic detector — every row of a table has the shape of every other row. `.fallowrc.jsonc` ignores both files whole, which also stops it seeing `Icon` and `addFromTapList`. Moving the data to its own module (as `src/db/schema-*.ts` already is) narrows the ignore to the table and puts the behaviour back in scope. | `src/ui/icons.tsx`, `src/modules/closet/tap-list.ts` | ready | Next time either file is touched — D-31 for icons, D-33 for the tap-list. |
| D-38 | ~~Mutation testing cannot work in the Workers pool.~~ **Solved, and it is fast.** Two lines in `vitest.config.ts`: forward `__STRYKER_ACTIVE_MUTANT__` into the pool as a binding (the pool's `process.env` is bindings, not inherited env — without it every mutant survives and the score is a meaningless 0.00), and `assetsInclude: ["**/*.bin"]` so stryker's own vitest can parse the photo fixture. That second line is what makes the *vitest* runner work rather than the `command` runner: 8m42s for `src/lib` instead of over an hour, plus per-test coverage and no false 0.00. `npm run mutate`. | `vitest.config.ts`, `stryker.conf.json` | done | — |
| D-40 | ~~87 surviving mutants in `src/lib`.~~ ~~The rest of `src/modules`.~~ **Closed. `src/lib` and every module under `src/modules` are at 100%** — `weather`, `ops`, `products`, `notifications`, `auth`, `closet`, `feed` and now `runs`. The mechanism is a ratchet with a single source of truth: `stryker.conf.json`'s `mutate` array is the list of scopes paid down to 100%, `break: 100` fails CI if one slips, and `.github/workflows/mutation.yml` reads that array and shards one job per entry so wall-clock stays flat as it grows. `runs` was the last and largest, 357 survivors down to none. What is still outside the gate is `.tsx` (D-42) and `src/modules/*/functions.ts`, which cannot be imported in the workers pool at all — that exclusion is now held honest by an architecture test rather than by trust (D-41). **`"stryker"` is now `required` in the commit gate** (2026-09-09). Two things surfaced when it was turned on, both recorded here because neither is visible in a diff: the analyzer **ignores this array** and mutates the changed `.ts`/`.tsx` files instead, so it blocks on `.tsx` (D-42) and on `src/modules/*/functions.ts` (65 live mutants on a file that is glue by design, with no grant available); and `stryker run` with no arguments was silently skipping the six array entries that contain a `!` negation — a bare local run measured 25 of 86 in-scope files, and only CI, which shards per entry through the CLI, ever checked the other six. `npm run mutate` is now a loop over the array, one stryker invocation per entry, exactly as CI shards it. | `src/modules/**` | done | Closed by the `runs` paydown. |
| D-41 | ~~`src/modules/*/functions.ts` cannot be mutation tested, and holds real logic.~~ **Closed.** A test that imports one fails with `Missing "#tanstack-router-entry" specifier in "@tanstack/start-server-core"` — `createServerFn` pulls in virtual entries only the dev/build pipeline provides. The answer, now applied everywhere: input schemas move to a sibling `inputs.ts` (importable, so testable), decisions move to plain siblings, the file is excluded from `stryker.conf.json`'s glob with a `!` negation, and `test/architecture/server-functions-are-glue.test.ts` keeps the exclusion honest — a file in this class may import, wire and delegate, and may not branch, loop, throw or declare a schema. The rule generalised on the way: the class is not "named functions.ts" but "cannot be imported in the workers pool", which the test determines by importing every module file and seeing which throw — that is how `auth/index.ts` was caught, a barrel with no TanStack import of its own that re-exports one. `NOT_YET_GLUE` is now empty, and a stale entry in it fails the test, so it cannot silently refill. The last thing to come out was the Strava OAuth CSRF state check, now `stravaCallbackOutcome` in `runs/strava/oauth.ts` with the forged-callback case asserted. | `src/modules/*/functions.ts` | done | Closed with the `runs` paydown. |
| D-43 | **A failed "mark all read" escapes as an unhandled rejection.** `NotificationList`'s handler is `try { … } finally { setIsMarking(false) }` with no `catch`, so a D1 failure re-throws out of a `void`-ed call: the button re-enables, the user is told nothing, and nothing reaches Sentry (laws 5 and 7). Found by writing the test for the release, which produced an unhandled rejection loud enough to fail stryker's dry run. Not fixed here because what the user should see is a product call — the smallest honest version is the failure band the forms contract already defines. | `modules/notifications/components/NotificationList.tsx` | ready | With the notifications lane, or any pass over the forms contract. |
| D-44 | **"Run logged." is announced into a live region that unmounts in the same commit.** `ManualRunForm`'s `onSuccess` sets the success sentence and then navigates to the new run, so the `role="status"` region is removed before a screen reader has any chance to read it. The forms contract's rule is *announce, then move*; this moves. Every other form in the app that navigates on success has the same shape, so the fix is probably in `useFormSubmit` — hold the navigation for a beat, or move the announcement to the destination. | `ui/use-form-submit.ts`, `modules/runs/components/ManualRunForm.tsx` | ready | With D-17's remaining form migrations. |
| D-42 | ~~The mutation ratchet covers `.ts` only, so no component is in it.~~ **Closed.** `src/ui/**/*.tsx` and `src/modules/**/*.tsx` are in the ratchet at 100% — **1,029 mutants across 23 component files**, plus the primitives. The objection that stopped this being obvious was mostly wrong: **stryker does not mutate a plain `className="..."` JSX attribute at all**, only the handful held in a const or built in an expression, and in `src/ui` every one of those carried a documented brand rule. What made it possible at all was the second vitest project (036): workerd has no DOM, so the worker pool could only `renderToString`. **Routes are excluded, and all 24 of them** — not just the 21 that fail to import, because the other three are route registration and `<head>` metadata whose 54 mutants are unreachable without instantiating the real router. The exclusion is held honest by `server-functions-are-glue`, which now covers `src/routes/**/*.tsx` and forbids a `.map(`, a JSX ternary and a JSX `&&` as well as the original list — so a decision cannot move back into a route without failing the build. Getting there moved roughly a thousand lines out of routes into ten components and five plain functions. | `stryker.conf.json` | done | Closed with the routes lane. |
| D-39 | ~~`.dependency-cruiser.cjs` does not exclude `.stryker-tmp/`.~~ **Done.** Stryker's sandbox is a full copy of the repo, so a run made the commit gate report `only-env-touches-cloudflare` against a *copied* `src/env/index.ts` — a real architecture rule naming a file nobody wrote. The exclude is one alternative in the existing path regex; verified by planting a sandbox copy and watching the gate flip both ways. `premutate`/`postmutate` still clear the directory, which is belt and braces for an interrupted run. Reported upstream as agentic-guardrails-scaffolding#55, since `guardrails init` seeds both this config and stryker's and the two disagreed. | `.dependency-cruiser.cjs` | done | — |
| D-35 | **Move `dupes` from `mild` to `semantic`, and pay the backlog it surfaces.** Measured: mild finds 3 of 6 clone groups confirmed real by reading them, and the three it misses are the dangerous ones. Semantic finds all six and reports 49 groups, ~17 distinct patterns, almost all in `modules/feed`. Not done with the analyzer's adoption because the gate reports clones in files a change *touches*: flipping it at the bottom of a stack makes five unrelated PRs each inherit a slice of a feed refactor. The flip and the extractions belong in one change. **Re-checked 2026-09-10, and a third of this row is stale.** The first of the three findings — "three feed routes each hand-rolling the same session redirect" — was fixed by the routes lane (#52): they all call `requireSignedIn` now. The other two stand: `feed/entries.ts` and `feed/photos.ts` still hand-roll the ownership check that `lib/owned.ts`'s `selectOwnedRow` exists for and `modules/runs` already uses, and `feed.ts`'s `garmentNameById` is `profiles.ts`'s `nameById`. **And the headline instruction contradicts CLAUDE.md**, which records that `semantic` "was tried first and rejected: it matches any two stretches of code with the same skeleton, and in a codebase whose modules are deliberately shaped alike that swamps the real findings." Both agree it reports ~49 groups; they disagree on whether that is worth it. **That is an owner decision and this row should not be actioned until it is made.** The two extractions above are worth doing either way and do not need the flip. | `.fallowrc.jsonc`, `src/modules/feed/**` | blocked | Owner decides `mild` vs `semantic`; the two extractions can proceed without it. |
| D-36 | **The form primitives have no ink surface.** The design specifies every one of them on dark as well as paper (`border 1px #2A2A31 -> 2px #F4F3EF`, same hi-viz band). No v1 form sits on ink, so it is unwritten rather than written and untested. | `src/ui/form.tsx` | ready | The first form on a dark surface. |
| D-37 | **`ui/Skeleton.tsx` uses `animate-pulse`.** The Motion Doctrine's NEVER list bans skeleton shimmer outright — "brackets breathe instead" — and the breathing device now exists in `motion.css`. Noticed while adding it. | `src/ui/Skeleton.tsx` | ready | Any time; it is a one-line swap plus a look at where skeletons render. |
| D-31 | **The tab bar renders text, not glyphs.** `TAB_BAR` in `ui/icons.tsx` now resolves all five tabs to a glyph, but `ui/TabBar.tsx` still renders mono uppercase labels alone. Wiring it is small and visible, so it wants its own demo re-record rather than riding along in an unrelated PR. | `src/ui/TabBar.tsx` | ready | With D-30, or the next PR that touches the shell. |
| D-32 | **Round 4's screens are designed and unbuilt.** Manual run entry (R1/R2), the notification list and bell states (S1/S2), Strava connect/import/disconnect (T1/T2/T3) and settings/privacy (U1/U2) all shipped placeholder-faithful in PR #4 and now have real artboards. W1/W2/W3 (report, block, faces-blurred-at-capture) belong to task 106; X (desktop feed) and Y1/Y2 (shoes as their own object) are post-v1. | `design/Remaining Screens.dc.html` | ready | Before the launch gate for R/S/T/U; with task 106 for W; unscheduled for X/Y. |

## Watch — correct for now

| # | What | Where | Trigger to revisit |
|---|---|---|---|
| D-20 | Per-garment thermal estimates do not compose. Wind resistance belongs to the outermost layer and modifies the whole stack, so a merino base under a shell is modelled backwards. Nothing computes a kit-level estimate yet, so nothing is wrong on screen. | `lib/thermal.ts:86` | When anything sums garments into a kit estimate. Owned by the call epic, which replaces these tables anyway. |
| D-21 | `waterResistant` is one boolean. Waterproof vs water-resistant is a real product distinction and probably belongs to enrichment (`fabric_composition`, D-34) rather than something a user grades. | `lib/contracts.ts` | When enrichment can supply it. |
| D-26 | An API would want a **payload fingerprint** alongside `runs.idempotency_key`. Today a repeat key returns the first run regardless of what the second request asked for — right for a browser resubmitting the same form, wrong for an API client that reused a key with a different body, where the correct answer is an error rather than someone else's run. Stripe stores a request hash for exactly this. No cleanup is needed either way: the key is a column on a row we keep, not a growing side table, so it dies with the run. | `modules/runs/service.ts` | If a public API is ever offered. |
| D-27 | Orphaned entry photos. `modules/feed/photos.ts` puts to R2 then inserts the row; a failure between leaves an object under a random key that nothing references, and MEDIA has no expiry so it never ages out (unlike IMPORTS, which has 30 days). Storage rather than correctness, and it needs D1 to fail between two calls. A sweep would compare keys under an entry prefix against `entry_photos`. | `modules/feed/photos.ts` | If storage ever looks wrong, or if photo volume gets serious. |
| D-22 | Search folds ASCII case only (`COLLATE NOCASE`), so accented names still miss. Fixing it needs stored normalisation, not a collation. | `modules/feed/search.ts` | First non-ASCII display name. |
| D-23 | Conditions freshness is 2 hour-buckets ≈ 60 min, the closest expressible value. A true 30-minute window needs sub-hour cache keys. | `modules/feed/conditions.ts` | If 60 minutes proves too stale in use. |
| D-29 | Demo journeys run at full speed in CI and are only paced when recording, so the slow path is never exercised automatically. | `playwright.config.ts` | If a demo ever breaks only at recording speed. |
| D-28 | E2E runs single-worker because every spec shares one dev server and one local D1 file. Parallelism needs a database per worker, which is cheap to run (empty SQLite file; ~210ms server boot) but not free to wire: local D1 lives under the vite plugin's `persistState` path, so it means N dev servers on N ports started from a globalSetup, plus a `vite.config.ts` change. Recipe is recorded in `playwright.config.ts`. | `playwright.config.ts`, `vite.config.ts` | When the suite is slow enough that ~11s serial stops being acceptable. |
