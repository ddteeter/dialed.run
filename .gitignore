node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.dev.vars*
!.dev.vars.example
!.env.example

# --- guardrails:start ---
.guardrails/state/*
!.guardrails/state/recurrence.json
reports/mutation/
reports/stryker-incremental.json
.stryker-tmp/
.claude/worktrees/
# --- guardrails:end ---

# playwright
test-results/
# Transcoded demo recordings: uploaded to the PR by `gh pr comment
# --attach`, which reads them from disk — they are never part of the repo.
demo.mp4
*.demo.mp4
playwright-report/
eval/pages/
# Photographs of people. The repo is public, and a corpus of running
# photos in git is a worse problem than the one it solves — see
# eval/photos/corpus.ts for the file names that belong here.
eval/photos/corpus/
